# Chirp

Chirp is a browser push-to-talk app. For reliable cross-device use, run or deploy the Node server so the app and PeerJS signaling are served from the same origin.

```powershell
npm.cmd install
npm.cmd start
```

Open the printed URL on each device. For phones and remote networks, deploy this repo to an HTTPS Node host and use that HTTPS URL.
